# 快速上传到 GitHub 指南

## ⚠️ 当前状态
检测到 Git 可能未安装或未配置到系统 PATH 中。

## 方案一：安装 Git 后使用命令行（推荐）

### 步骤 1：安装 Git
1. 访问 https://git-scm.com/download/win
2. 下载并安装 Git for Windows
3. **安装时选择 "Add Git to PATH"**（重要！）
4. 安装完成后，**重启命令提示符或 PowerShell**

### 步骤 2：配置 Git（首次使用）
打开命令提示符或 PowerShell，执行：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

### 步骤 3：在项目文件夹中执行以下命令

**方法 A：使用我创建的脚本**
1. 在项目文件夹中，按住 Shift + 右键
2. 选择"在此处打开 PowerShell 窗口"或"在此处打开命令窗口"
3. 执行：`powershell -ExecutionPolicy Bypass -File upload_to_github.ps1`

**方法 B：手动执行命令**
```bash
# 1. 初始化仓库（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 创建提交
git commit -m "Initial commit: Gunpla Price Tool"

# 4. 在 GitHub 上创建新仓库后，连接远程仓库
#    替换下面的 URL 为你的仓库地址
git remote add origin https://github.com/你的用户名/仓库名.git

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

---

## 方案二：使用 GitHub Desktop（最简单，无需命令行）

### 步骤 1：下载 GitHub Desktop
访问 https://desktop.github.com/ 下载并安装

### 步骤 2：登录 GitHub 账户
打开 GitHub Desktop，使用你的 GitHub 账户登录

### 步骤 3：添加本地仓库
1. 点击 File > Add Local Repository
2. 点击 "Choose..." 按钮
3. 选择项目文件夹：`C:\Users\bixi\Desktop\学习\project2-1\gunpla_price_tool`
4. 点击 "Add repository"

### 步骤 4：提交更改
1. 在左侧会显示所有更改的文件
2. 在底部输入提交信息：`Initial commit: Gunpla Price Tool`
3. 点击 "Commit to main" 按钮

### 步骤 5：发布到 GitHub
1. 点击 "Publish repository" 按钮（在顶部工具栏）
2. 输入仓库名称（例如：`gunpla_price_tool`）
3. 可以添加描述（可选）
4. 选择是否公开（Private 或 Public）
5. 点击 "Publish Repository"

完成！你的代码已经上传到 GitHub 了。

---

## 在 GitHub 上创建仓库（如果使用命令行）

1. 登录 https://github.com
2. 点击右上角的 "+" 号
3. 选择 "New repository"
4. 输入仓库名称（例如：`gunpla_price_tool`）
5. **不要**勾选 "Initialize this repository with a README"（因为本地已有代码）
6. 点击 "Create repository"
7. 复制显示的仓库 URL（例如：`https://github.com/你的用户名/gunpla_price_tool.git`）

---

## GitHub 认证说明

GitHub 已不再支持密码认证，需要使用以下方式之一：

### 方式 1：Personal Access Token (PAT)
1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" > "Generate new token (classic)"
3. 输入名称，选择过期时间
4. 勾选 `repo` 权限
5. 点击 "Generate token"
6. **复制生成的 token**（只显示一次！）
7. 推送时，用户名输入你的 GitHub 用户名，密码输入这个 token

### 方式 2：SSH 密钥（推荐用于长期使用）
1. 生成 SSH 密钥：`ssh-keygen -t ed25519 -C "你的邮箱"`
2. 将公钥添加到 GitHub：Settings > SSH and GPG keys > New SSH key
3. 使用 SSH URL：`git@github.com:你的用户名/仓库名.git`

---

## 需要帮助？

如果遇到问题，请查看 `GITHUB_UPLOAD_GUIDE.md` 获取更详细的说明。
